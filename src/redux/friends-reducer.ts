
const initialState = {
    names: [
        {id: 1, name: 'Andrew'},
        {id: 2, name: 'Vano'},
        {id: 3, name: 'Tony'}
    ]
}
const friendsReducer = (state = initialState, action: any) => {

    return state;
};

export default friendsReducer;